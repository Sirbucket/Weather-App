export const itemTemplate = document.querySelector("#weatheritem"); 
//assign this.
export const errormsg: HTMLElement = document.querySelector(".error .errormsg");

export const customErrorMessages = {
    validityError: "Please give a valid longitude and latitude!",
}